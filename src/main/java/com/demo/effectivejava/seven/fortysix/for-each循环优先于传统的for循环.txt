使用for-each循环比起普通的for循环，还稍有性能优势，它对数组索引的边界值计算，只计算一次，可以手工完成这项工作，但程序员并不总是这么做。

3种情况无法使用for-each循环
1,过滤，如果需要遍历集合，并删除选定的元素，就需要使用显式的迭代器，以便可以调用它的remove方法
2，转换，如果需要遍历列表或者数组，并取代它部分或者全部的元素值，就需要列表迭代器或者数组索引，以便设定元素的值。
3，平行迭代，如果需要并行地遍历多个集合，就需要显式地控制迭代器或者索引变量，以便所有迭代器或者索引变量都 可以得到同步前移。